/*
 * TransferList Messages
 *
 * This contains all the text for the TransferList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TransferList';

export default defineMessages({

});

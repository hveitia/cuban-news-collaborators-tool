/*
 * CheckboxComponent Messages
 *
 * This contains all the text for the CheckboxComponent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CheckboxComponent';

export default defineMessages({
 
});

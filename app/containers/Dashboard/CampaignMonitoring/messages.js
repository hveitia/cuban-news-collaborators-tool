/*
 * CampaignMonitoring Messages
 *
 * This contains all the text for the CampaignMonitoring container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CampaignMonitoring';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CampaignMonitoring container!',
  },
});

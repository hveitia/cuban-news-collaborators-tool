/*
 * BankingSystem Messages
 *
 * This contains all the text for the BankingSystem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BankingSystem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BankingSystem container!',
  },
});

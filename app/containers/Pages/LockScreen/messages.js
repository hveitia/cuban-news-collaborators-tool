/*
 * LockScreen Messages
 *
 * This contains all the text for the LockScreen container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LockScreen';

export default defineMessages({

});

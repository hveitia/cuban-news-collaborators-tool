// import { selectForumsComponentDomain } from '../selectors';

describe('selectForumsComponentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

/*
 * InvoiceLists Messages
 *
 * This contains all the text for the InvoiceLists container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InvoiceLists';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InvoiceLists container!',
  },
});
